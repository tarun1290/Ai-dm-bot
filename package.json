{
  "name": "query-bot-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "dashboard",
    "query-bot"
  ],
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "install-all": "npm install && cd dashboard && npm install && cd ../query-bot && npm install"
  }
}
